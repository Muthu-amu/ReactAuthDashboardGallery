import React from 'react';

const AccountSettings = () => {
  return (
    <div>
      <h2>Account Settings</h2>
      {/* Add form elements to update password and Spanish name here */}
    </div>
  );
};

export default AccountSettings;
